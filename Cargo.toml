[package]
name = "rust-rf-decoder"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
num = "0.4.1"

[dev-dependencies]
plotly = { version = "0.8.4", features = ["kaleido"] }
